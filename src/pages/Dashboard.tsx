const Dashboard = () => {
  return (
    <div style={{ padding: '20px', textAlign: 'center' }}>
      <h1>📊 Dashboard</h1>
      <p>Vista del Dashboard, accesible en la ruta "/dashboard".</p>
    </div>
  );
};

export default Dashboard;